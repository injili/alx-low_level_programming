#include "main.h"

/**
 * _memset - function that fills the first n bytes of the memory
 * @s: pointer to the memory area
 * @b: the constant byte
 * @n: number of memory spaces
 *
 * Return: on succcess a pointer to memory area s
 */

char *_memset(char *s, char b, unsigned int n)
{

	return ();
}
