#include <stdio.h>

/**
 * main - the main and only function of the program
 * @void: the main function receives no argument
 *
 * Return: 0 on successful execution
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
